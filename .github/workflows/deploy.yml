# Этот файл — шаблон для ручного добавления через GitHub UI.
# Создайте его вручную на github.com → ваш репозиторий → Actions → New workflow.
# Содержимое шаблона находится в файле: docs/github-actions-deploy.yml
